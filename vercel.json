{
  "buildCommand": "cd client && npx vite build && node -e \"const fs=require('fs');fs.cpSync('../client/dist','../api/public',{recursive:true,force:true})\"",
  "installCommand": "npm install && cd client && npm install",
  "rewrites": [
    { "source": "/(.*)", "destination": "/api" }
  ],
  "functions": {
    "api/index.js": {
      "maxDuration": 60,
      "includeFiles": "api/public/**"
    }
  }
}
