{
  "installCommand": "npm ci && cd client && npm ci",
  "buildCommand": "cd client && npx vite build && cd .. && node -e \"const fs=require('fs');fs.cpSync('client/dist','api/public',{recursive:true,force:true})\"",
  "rewrites": [
    { "source": "/(.*)", "destination": "/api" }
  ],
  "functions": {
    "api/index.js": {
      "maxDuration": 60,
      "includeFiles": "api/public/**"
    }
  }
}
